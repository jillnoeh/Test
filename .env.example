ANTHROPIC_API_KEY=sk-ant-...
PORT=3000

# Password required to use the app. Defaults to "storysprout123" if unset.
# Change this on your hosted deployment (e.g. in Render's env vars).
APP_PASSWORD=storysprout123
